import React from 'react';
import { Grid, Typography } from '@mui/material';

const TrackingPage = () => (
  <Grid>
    <Typography>Tracking Page</Typography>
  </Grid>
);

export default TrackingPage;
