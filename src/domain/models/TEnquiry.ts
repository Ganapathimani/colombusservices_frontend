export type TEnquiry = {
  id?: string,
  company: string,
  email: string,
  file: string | null,
  message: string,
  name: string,
  phone: string,
  pickupId?: string,
  role: string,
  status?: string,
};
