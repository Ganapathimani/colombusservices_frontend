export type TSignup = {
  name: string;
  email: string;
  password: string;
  companyName: string;
  gstNumber: string;
  location: string;
  mobile: string;
  role?: string,
  adminSecret?: string,
};
